# Streamlit Secrets Example
# Copy this to .streamlit/secrets.toml for local development
# For Streamlit Cloud, add secrets via the dashboard

# MLflow Configuration (if using remote MLflow)
# MLFLOW_TRACKING_URI = "s3://your-bucket/mlflow"

# Model Configuration
# MODEL_PATH = "models/model.joblib"

